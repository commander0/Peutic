# =======================================================================
# PEUTIC - PRODUCTION ENVIRONMENT VARIABLES GUIDE
# =======================================================================

# -----------------------------------------------------------------------
# 1. SUPABASE CONNECTION [CRITICAL SCALABILITY SETTING]
# -----------------------------------------------------------------------
# IMPORTANT: For production (global launch), you MUST use the 
# Supabase Transaction Pooler URL (usually port 6543). 
# DO NOT use the direct connection (port 5432) or the app will drop 
# connections under viral load (100k+ MAU).
#
# Reference URL Format: postgres://[user].[project]:[password]@[aws-region].pooler.supabase.com:6543/postgres
VITE_SUPABASE_URL=https://[YOUR_PROJECT_REF].supabase.co
VITE_SUPABASE_ANON_KEY=ey...

# -----------------------------------------------------------------------
# 2. PAYMENT GATEWAY (STRIPE)
# -----------------------------------------------------------------------
# Must be your LIVE key for global launch.
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_...

# -----------------------------------------------------------------------
# 3. AI COMPANION (TAVUS & OPENAI)
# -----------------------------------------------------------------------
VITE_TAVUS_API_KEY=your_tavus_api_key

# -----------------------------------------------------------------------
# 4. REDIS EDGE CACHING (PERFORMANCE)
# -----------------------------------------------------------------------
VITE_REDIS_URL=your_redis_url
VITE_REDIS_TOKEN=your_redis_token

# -----------------------------------------------------------------------
# 5. BUG TRACKING & MONITORING (SENTRY)
# -----------------------------------------------------------------------
VITE_SENTRY_DSN=your_sentry_dsn_here
